spring:
    application:
        name: sivad-catalogos
    profiles:
        active: #spring.profiles.active#
    config:
        location: classpath:/core-application.yml,classpath:/application.yml
    cloud:
        config:
            uri: https://nmp-config-server.cfapps.io
            password: password
